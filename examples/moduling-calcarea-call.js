/**
 * Please remember that if you want to run this code on node, 
 * you should use a compiler/translator (e.g babeljs)
 */

import { circleArea, squareArea } from './moduling-calcarea';
console.log(circleArea(2));
console.log(squareArea(2));